{
  "_comment": [
    "The porting ledger. Each key is a WinSCP translation unit (path stem under",
    "vendor/winscp/source, without extension). Values:",
    "  status  - done | partial | replaced | na | todo",
    "  targets - files in THIS repository carrying the behaviour. A target that",
    "            does not exist is reported as broken and counts as zero.",
    "  progress- for 'partial' only, 0..1; defaults to 0.5",
    "  note    - short, factual",
    "Never mark a unit done to move on. See docs/porting-mandate.md."
  ],

  "units": {
    "core/Configuration": {
      "status": "partial",
      "progress": 0.6,
      "targets": ["design/main/config.js", "design/main/defaults.js", "design/main/paths.js"],
      "note": "Store, atomic writes, defaults. INI import/export still to do."
    },
    "core/SessionData": {
      "status": "partial",
      "progress": 0.5,
      "targets": ["design/main/defaults.js", "design/main/config.js"],
      "note": "Full option set declared; URL parsing and raw-settings editing pending."
    },
    "core/Security": {
      "status": "partial",
      "progress": 0.6,
      "targets": ["design/main/crypto.js"],
      "note": "Credential protection and master password. WinSCP password obfuscation compat pending."
    },
    "core/Cryptography": {
      "status": "partial",
      "progress": 0.4,
      "targets": ["design/main/crypto.js"],
      "note": "AES-GCM + scrypt in place; file-encryption feature pending."
    },
    "core/HierarchicalStorage": {
      "status": "partial",
      "progress": 0.5,
      "targets": ["design/main/config.js"],
      "note": "JSON store replaces INI/registry hierarchy; INI compatibility layer pending."
    },
    "forms/NonVisual": {
      "status": "partial",
      "progress": 0.5,
      "targets": ["tools/extract-actions.js", "design/renderer/actions.js"],
      "note": "All 301 actions extracted from the original with captions, hints and shortcuts; handlers pending."
    },

    "core/SftpFileSystem": {
      "status": "partial",
      "progress": 0.45,
      "targets": ["design/main/protocols/sftp.js"],
      "note": "SFTP over ssh2: auth, tunnels, proxies, listing, transfers with resume. Bug-workaround matrix still to verify against a real server."
    },
    "core/ScpFileSystem": {
      "status": "partial",
      "progress": 0.4,
      "targets": ["design/main/protocols/scp.js"],
      "note": "SCP over the shared SSH transport, with listing-command parsing."
    },
    "core/SecureShell": {
      "status": "partial",
      "progress": 0.4,
      "targets": ["design/main/protocols/sftp.js"],
      "note": "Transport, host-key verification and keepalives live with the SSH adapter."
    },
    "core/FtpFileSystem": {
      "status": "partial",
      "progress": 0.4,
      "targets": ["design/main/protocols/ftp.js"],
      "note": "FTP/FTPS over basic-ftp; MLSD and multi-dialect LIST parsing covered by tests."
    },
    "core/WebDAVFileSystem": {
      "status": "partial",
      "progress": 0.45,
      "targets": ["design/main/protocols/webdav.js"],
      "note": "Hand-written client; PROPFIND parsing and RFC 7616 Digest auth covered by tests."
    },
    "core/S3FileSystem": {
      "status": "partial",
      "progress": 0.5,
      "targets": ["design/main/protocols/s3.js"],
      "note": "Hand-written SigV4 verified against published AWS vectors; multipart, paging, AssumeRole. No live endpoint tested yet."
    },
    "core/NeonIntf": {
      "status": "replaced",
      "targets": ["design/main/protocols/webdav.js"],
      "note": "neon (the C WebDAV/HTTP library WinSCP links) is replaced by a hand-written client on node:http/https."
    },
    "core/Http": {
      "status": "partial",
      "progress": 0.5,
      "targets": ["design/main/protocols/webdav.js", "design/main/protocols/s3.js"],
      "note": "HTTP plumbing lives with the two protocols that need it."
    },
    "core/FileMasks": {
      "status": "partial",
      "progress": 0.7,
      "targets": ["design/main/masks.js"],
      "note": "WinSCP mask syntax including size and time masks, with a test table."
    },
    "core/Queue": {
      "status": "partial",
      "progress": 0.4,
      "targets": ["design/main/queue.js"],
      "note": "Parallel transfers, pause/resume, throttling, overwrite queries."
    },
    "core/CopyParam": {
      "status": "partial",
      "progress": 0.5,
      "targets": ["design/main/queue.js", "design/main/defaults.js"],
      "note": "Transfer parameters declared and applied by the queue."
    },
    "core/FileOperationProgress": {
      "status": "partial",
      "progress": 0.4,
      "targets": ["design/main/queue.js"],
      "note": "Byte/file counters, throughput and ETA reporting."
    },
    "forms/FileFind": {
      "status": "partial",
      "progress": 0.4,
      "targets": ["design/main/find.js"],
      "note": "Streaming recursive search with mask + content grep. 17 tests. The dialog itself is not built yet."
    },
    "windows/SynchronizeController": {
      "status": "partial",
      "progress": 0.5,
      "targets": ["design/main/sync.js"],
      "note": "Comparison checklist across all direction/mode/criteria combinations, DST handling, keep-up-to-date watching."
    },
    "core/Terminal": {
      "status": "partial",
      "progress": 0.15,
      "targets": ["design/main/session.js", "design/main/sync.js"],
      "note": "Session lifecycle and the synchronization engine. Terminal is 7,000+ lines; most of it is still ahead."
    },
    "windows/TerminalManager": {
      "status": "partial",
      "progress": 0.35,
      "targets": ["design/main/session.js"],
      "note": "Multiple sessions, reconnect policy, verification callbacks."
    },
    "windows/EditorManager": {
      "status": "partial",
      "progress": 0.4,
      "targets": ["design/main/editors.js"],
      "note": "Internal and external editor lifecycle with upload-on-save."
    },

    "forms/Glyphs": {
      "status": "replaced",
      "targets": ["design/renderer/ui/commands.js"],
      "note": "Embedded PNG bitmap sheet (7,981 lines, ~90% pure hex; the .cpp behind it is 14 lines). Material Symbols replace it — the action-to-icon mapping is iconFor() in commands.js."
    },
    "forms/Glyphs120": {
      "status": "replaced",
      "targets": ["design/renderer/ui/commands.js"],
      "note": "120 DPI variant of the same bitmap sheet. A vector icon set has no DPI variants, so the four sheets collapse into one mapping."
    },
    "forms/Glyphs144": {
      "status": "replaced",
      "targets": ["design/renderer/ui/commands.js"],
      "note": "144 DPI variant of the same bitmap sheet."
    },
    "forms/Glyphs192": {
      "status": "replaced",
      "targets": ["design/renderer/ui/commands.js"],
      "note": "192 DPI variant of the same bitmap sheet (9,123 of 9,916 lines are pure hex)."
    },
    "forms/Animations96": {
      "status": "replaced",
      "targets": ["design/renderer/styles/components.css"],
      "note": "Embedded animation frame bitmaps. M3 motion is expressed in CSS, so frame sheets are not transcribed."
    },
    "forms/Animations120": {
      "status": "replaced",
      "targets": ["design/renderer/styles/components.css"],
      "note": "120 DPI animation frame bitmaps."
    },
    "forms/Animations144": {
      "status": "replaced",
      "targets": ["design/renderer/styles/components.css"],
      "note": "144 DPI animation frame bitmaps (15,747 of 18,155 lines are pure hex)."
    },
    "forms/Animations192": {
      "status": "replaced",
      "targets": ["design/renderer/styles/components.css"],
      "note": "192 DPI animation frame bitmaps."
    },

    "putty/putty": { "status": "na", "note": "Directory-level replacement is recorded separately, not per unit." },
    "filezilla/filezilla": { "status": "na", "note": "Directory-level replacement is recorded separately, not per unit." }
  }
}
